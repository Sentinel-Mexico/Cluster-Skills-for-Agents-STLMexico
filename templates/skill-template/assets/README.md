# Assets Directory

Place static assets, JSON schemas, templates, example configurations, and media resources in this directory.

## Standards
- Keep files structured and version-controlled.
- Reference these assets within `SKILL.md` using relative paths (e.g. `assets/schema.json`).
